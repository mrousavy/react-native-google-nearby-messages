{
  "name": "example",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "android": "cd .. && tsc --noEmit false && rm -rf node_modules && cd example && rm -rf node_modules && yarn && react-native run-android",
    "ios": "cd .. && tsc --noEmit false && rm -rf node_modules && cd example && rm -rf node_modules && yarn && cd ios && pod install && cd .. && react-native run-ios",
    "start": "react-native start",
    "test": "jest",
    "lint": "eslint ."
  },
  "dependencies": {
    "react": "16.11.0",
    "react-native": "0.62.2",
    "react-native-google-nearby-messages": "link:../"
  },
  "devDependencies": {
    "@babel/core": "^7.10.2",
    "@babel/runtime": "^7.10.2",
    "@react-native-community/eslint-config": "^1.1.0",
    "babel-jest": "^26.0.1",
    "eslint": "^7.1.0",
    "jest": "^26.0.1",
    "metro-react-native-babel-preset": "^0.59.0",
    "react-test-renderer": "16.11.0"
  },
  "jest": {
    "preset": "react-native"
  }
}
